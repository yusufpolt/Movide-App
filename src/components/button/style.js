import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 10,
    width: '100%',
    backgroundColor: '#4b91ff',
    padding: 20,
  },
  buttonText: {
    textAlign: 'center',
    fontSize: 20,
    fontWeight: '600',
    color: '#fff',
  },
});
